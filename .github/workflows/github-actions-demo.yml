name: Github Actions Demo
on:
  push:
    branches:
      - main
jobs:
  greeting:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name == 'main' && 'main' || 'development' }}
    steps:
      - name: Print greeting
        run: echo "${{ vars.E_TEST }}"
